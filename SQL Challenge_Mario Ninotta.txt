select 
      ROUND((a.column_1/2),0) as cl1,
      b.email,
      a.column_3
  from public.data_table a 
  inner join public.email_table b
     on a.join_id = b.join_id 
  where
      a.column_2 < CL1
     AND a.column_3 like '%1' ;